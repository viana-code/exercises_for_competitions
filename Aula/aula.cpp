#include <bits/stdc++.h>

using namespace std;

typedef long long int ll;
#define faster ios::sync_with_stdio(0); cin.tie(0);

int32_t main() { faster
    ll a, b, c;

    while(cin >> a >> b >> c) {
        cout << (a+b+c) << endl;
    }
    
    return 0;
}