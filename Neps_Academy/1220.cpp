#include <bits/stdc++.h>

using namespace std;

typedef long long int ll;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    ll N, A;
    string S;

    for(int i = 0; i < N; i++) {
        cin >> A;
        cin.ignore();
        getline(cin, S);
    }
    
    return 0;
}